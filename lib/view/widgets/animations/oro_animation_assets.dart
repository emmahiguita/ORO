class OroAnimationAssets {
  OroAnimationAssets._();

  static const String prefix = 'lottie/';

  /// Animación de carga y sincronización
  static const String loading = '${prefix}loading.json';

  /// Animación de carrito vacío o sin resultados
  static const String empty = '${prefix}empty.json';

  /// Animación de confirmación de orden y compra exitosa
  static const String orderPlaced = '${prefix}orederplaced.json';

  /// Animación de campana y notificaciones
  static const String notification = '${prefix}notification.json';

  /// Animación de reseñas y estrellas de calificación
  static const String review = '${prefix}review.json';

  /// Animación de modo fuera de línea / sin conexión
  static const String offline = '${prefix}offline.json';

  /// Animación de error 404 / no encontrado
  static const String notFound = '${prefix}404.json';

  /// Animación de lista vacía o sin datos
  static const String noData = '${prefix}nodata.json';
}
